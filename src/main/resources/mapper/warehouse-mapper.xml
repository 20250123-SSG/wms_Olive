<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.minisec.warehouse.model.dao.ProductMapper">

    <resultMap id="productResultMap" type="ProductDto">
        <id column="warehouse_detail_id" property="warehouseDetailId"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="product_id" property="productId"/>
        <result column="warehouse_detail_quantity" property="warehouseDetailQuantity"/>
        <result column="created_at" property="createdAt"/>
        <result column="modified_at" property="modifiedAt"/>
    </resultMap>
    <!-- 전체 상품 목록 조회 -->
    <select id="getProductList" resultMap="productResultMap">
        SELECT
            warehouse_detail_id,
            warehouse_id,
            product_id,
            warehouse_detail_quantity,
            created_at,
            modified_at
        FROM
            warehouse_detail
    </select>

</mapper>
